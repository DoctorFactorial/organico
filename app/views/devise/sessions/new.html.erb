<div class="login-background">

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-signin"}) do |f| %>
	
	<h2 class="center_subheader">Iniciar sesión</h2>

    <%= f.input :email, required: false, autofocus: true, label: false, placeholder: "Correo electrónico", input_html: {class: "form-control"} %>
    <%= f.input :password, required: false, autofocus: true, label: false, placeholder: "Contraseña", input_html: {class: "form-control"} %>

    <%= f.button :submit, "Iniciar sesión", class: "btn  btn-success btn-block" %>
	<div class="text-center">
    <%= render "devise/shared/links"%>
	</div>
<% end %>

</div>
