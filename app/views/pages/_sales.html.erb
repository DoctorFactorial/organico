<table class="table table-condensed">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Fecha</th>
      <th>Fecha</th>
      <th>Comprador</th>
    </tr>
  </thead>
  <tbody>
  <% @purchased.each do |sale| %>
    <tr>
      <td><%= link_to sale.item.name, pickup_path(sale.guid) %></td>
      <td><%= time_ago_in_words(sale.created_at) %> ago</td>  
      <td><%= sale.buyer.email %> ago</td>  
    </tr>
	<% end %>
</tbody>
</table>